# MVAR — MIRRA Verified Agent Runtime
# Production dependencies with pinned versions

# Core dependencies
numpy>=1.26.0,<3.0.0
cryptography==46.0.7
prometheus-client>=0.20.0,<1.0.0
opentelemetry-api>=1.24.0,<2.0.0
opentelemetry-sdk>=1.24.0,<2.0.0

# Optional: For development/testing only
# pytest>=7.4.0,<8.0.0  # Uncomment for testing
